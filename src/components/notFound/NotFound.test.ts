// import "@testing-library/jest-dom"
// import "@testing-library/jest-dom/jest-globals"
// import { render } from "@testing-library/react"

// const { expect } = require("@jest/globals")

// describe("NotFound", function () {
//   test("renders NotFound component correctly", () => {
//     const { getByText, getByAltText } = render(<NotFound />)

//     // Verifica que el texto y la imagen se rendericen correctamente
//     const pageTitle = getByText(/PAGE NOT FOUND/i)
//     const pageSubtitle = getByText(/Please, come back to main page/i)
//     const imageAltText = getByAltText("Groot")

//     expect(pageTitle).toBeInTheDocument()
//     expect(pageSubtitle).toBeInTheDocument()
//     expect(imageAltText).toBeInTheDocument()
//   })
// })
